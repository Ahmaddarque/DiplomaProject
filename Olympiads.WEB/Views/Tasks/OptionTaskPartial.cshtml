@model IEnumerable<string>

<div id="option-task">
    @foreach (var option in Model)
    {
        <div class="form-check">
            <input type="radio" class="form-check-input option" />
            <label class="form-check-label">@option</label>
        </div>
    }
</div>